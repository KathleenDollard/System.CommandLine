using System.Collections.Generic;

namespace System.CommandLine.API
{
    public static class CommandLineExtensions
    {
        //public static CommandLineResult Parse(this CommandLine command, string input)
        //{
        //    var commandDefinition = ParserWrapper.CreateDefinition(command);
        //    // I don't like this semantics or extra step. I think the API creates the definition
        //    var parser = new System.CommandLine.Parser.Parser(commandDefinition);
        //    return parser.Parse(input)
        //}

        //public static CommandLineResult Parse(params string[] input)
        //{
        //    var commandDefinition = ParserWrapper.CreateDefinition(_command);
        //    // I don't like this semantics or extra step. I think the API creates the definition
        //    var parser = new System.CommandLine.Parser.Parser(commandDefinition);
        //    return parser.Parse(input)
        //}

    }


}
